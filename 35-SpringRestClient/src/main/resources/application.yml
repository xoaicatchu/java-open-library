server:
  port: 8135
spring:
  threads:
    virtual:
      enabled: true
  datasource:
    url: jdbc:h2:mem:restclientdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
